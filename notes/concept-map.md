# Concept Map

A running synthesis log. After every topic, append one paragraph using the pattern:

> **[Topic ID]** — what this depends on, what it changes about my view of those prerequisites, what new problem it creates that the next topic will address.

Not a summary. A *connection*. If you find yourself writing "I learned X," rewrite it as "X is a response to Y, and it forces Z."

---

<!-- Append entries below, newest at the bottom. -->
A1 -> Created Sockets (communication endpoints)
A2 -> Added control for the number of queued-up connections using the queue backlog. Dequeued by `accept()`ing.
A3 -> 